# Handoff — текущее состояние

## Что сделано (закоммичено)
- **H6+H7** (`eee5c97`): `reindex` фоновый (daemon-поток) + `reindex_status`; токен-аутентификация `auth_token`/`ONEC_AUTH_TOKEN` через `StaticTokenVerifier`.
- **Инкрементальность при прерывании** (`d2f796f`): хэши узлов пишутся только после успешного `upsert` батча в Qdrant; `config_checksum` записывается после эмбеддинга. Прерванный прогон продолжается с места при следующем инкрементальном запуске.
- **Дубли ID в отчёте** (`d2f796f`): `report_parser` теперь корректно вкладывает реквизиты табличных частей (elem_type по последнему контейнеру сегмента + выталкивание `elem_stack`). Устранено 1400 дублей — 25760 уникальных ID.

## Текущее состояние индекса
- Qdrant: 25760 точек, SQLite-граф: 25760 узлов / 28747 рёбер, 2975 объектов.
- Поиск работает: «цена товара» → `Стоимость`, «контрагент» → `КПП`/`ИНН` и т.д.
- **`found 0 BSL files`** — BSL не индексируется: `config_root` указывает на `data/AKADA/metadata`, а код лежит в `data/AKADA/code/`. Граф вызовов (CALLS/USES) пуст.

## Известные ограничения / не сделано
- **BSL-парсинг** (сознательно отложен): нужно направить `config_root` на `data/AKADA/code` (или пересмотреть логику поиска `.bsl`/`.bs`) и проверить `infer_module_role` под реальную структуру `code/`.
- Мелкие дубли данных в исходном отчёте (`Стоимость` в двух объектах) — не баг ID, на поиск не влияет.
- `config.json` содержит реальные IP (192.168.31.31/34) — локальная настройка, не коммитить.

## Инфраструктура
- Эмбеддер `192.168.31.34:8081` (llama.cpp, `jina-code-embeddings-1.5b-GGUF`, dim 1536) — работает.
- Qdrant `192.168.31.31:6333` — работает.

## Следующий шаг
1. Подключить BSL: разобраться со структурой `data/AKADA/code/`, поправить поиск `.bsl` и `infer_module_role`.
2. После этого — полная переиндексация с векторами (нагрузка ~20 мин, эмбеддер ~12 vec/s).
